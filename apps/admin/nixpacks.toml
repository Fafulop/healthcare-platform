[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
cmds = [
  "cd ../.. && pnpm install --frozen-lockfile"
]

[phases.build]
cmds = [
  "cd ../.. && pnpm --filter @healthcare/admin build"
]

[start]
cmd = "cd ../.. && pnpm --filter @healthcare/admin start"
